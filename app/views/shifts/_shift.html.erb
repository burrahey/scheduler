<%= shift.start_time.strftime("%l:%M %P") %> - <%= shift.end_time.strftime("%l:%M %P") %> on <%= shift.date.strftime("%A, %B %e, %Y") %> <strong><%= "| PUBLISHED" if shift.published %></strong>
--
<%= link_to "Edit", edit_admin_employee_shift_path(shift.employee, shift) %> | <%= link_to "Delete", admin_employee_shift_path(shift.employee, shift), method: 'delete' %>
<br />
