<div class='container'>
  <h2>Edit shift here:</h2>
  <%= form_for [:admin, :employee, @shift] do |t| %>
    <%= render "form", t: t %>
    <div class="submit-button">
      <%= t.submit %>
    </div>
  <% end %>
</div>
