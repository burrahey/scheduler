<strong> <%= shift.start_time.strftime("%l:%M %P") %> - <%= shift.end_time.strftime("%l:%M %P") %> on <%= shift.date.strftime("%A, %B %e, %Y") %> </strong>
--
<%= link_to "Edit", edit_admin_employee_shift_path(shift.employee, shift) %> | <%= link_to "Delete", admin_employee_shift_path(shift.employee, shift), method: 'delete' %> <strong><%= "| PUBLISHED" if shift.published %></strong>
<br />
