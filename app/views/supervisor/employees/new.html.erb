<h2>Add a New Employee:</h2>
<div class='container'>
  <%= form_for [:supervisor, @employee] do |f| %>
    <%= render partial: 'form', locals: {employee: @employee, f:f} %>
      <div class="submit-button">
        <%= f.submit %>
      </div>
  <% end %>
</div>
